# NPM 将所有依赖包版本升级到最新

1. 安装 `npm-check-updates`

    ```shell
    $ npm install -g npm-check-updates
    ```

2. 查看哪些包可以升级

    ```shell
    $ npm-check-updates
    ```

3. 将所有包升级到最新

    ```shell
    $ npm-check-updates -u
    $ npm i
    ```